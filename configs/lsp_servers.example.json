{
  "version": 1,
  "baselineServer": "pylance",
  "servers": [
    {
      "id": "pylance",
      "displayName": "Pylance",
      "enabled": true,
      "kind": "node-wrapper",
      "sourcePath": "C:/path/to/pylance-server/dist/server.js",
      "launch": {
        "command": "C:/Program Files/nodejs/node.exe",
        "args": [
          "../tools/pylance_stdio_launcher.cjs",
          "--server-path",
          "C:/path/to/pylance-server/dist/server.js"
        ]
      },
      "notes": [
        "Copy this file to .python-lsp-compare/lsp_servers.json and replace placeholder paths.",
        "The launcher script path may stay relative to the config file."
      ]
    },
    {
      "id": "ty",
      "displayName": "Ty",
      "enabled": true,
      "kind": "native-exe",
      "sourcePath": "C:/path/to/ty.exe",
      "launch": {
        "command": "C:/path/to/ty.exe",
        "args": [
          "server"
        ]
      },
      "notes": [
        "Ty uses the `server` subcommand for LSP."
      ]
    },
    {
      "id": "pyrefly",
      "displayName": "Pyrefly",
      "enabled": true,
      "kind": "native-exe",
      "sourcePath": "C:/path/to/pyrefly.exe",
      "launch": {
        "command": "C:/path/to/pyrefly.exe",
        "args": [
          "lsp",
          "--indexing-mode",
          "lazy-blocking",
          "--build-system-blocking"
        ]
      },
      "notes": [
        "Pyrefly is typically launched with `lsp --indexing-mode lazy-blocking --build-system-blocking`.",
        "If a server truly needs different arguments for benchmarks only, use launch.benchmarkArgs as an optional advanced field."
      ]
    }
  ]
}